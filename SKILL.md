---
name: novelclaw
description: AI 长篇小说写作助手。用于：网文/小说创作、章节生成、人物管理、世界观设定、文风切换（打斗/情感/悬疑）、自进化避雷。触发词：写小说、写章节、小说助手、novelclaw、网文创作、人物设定、世界观、帮我写小说、续写。
---

# NovelClaw — 自进化 AI 写作助手

## 项目目录结构

```
novel/
├── config.json          # 项目配置（标题、类型、字数目标）
├── characters.json      # 人物志
├── world.json           # 世界观设定
├── style_constraints.json  # 自进化避雷词库
├── chapters/            # 已写章节 (001.md, 002.md, ...)
├── outlines/
│   ├── outline.md       # 总大纲
│   └── summary_log.md   # 章节摘要
```

## 工作流

### 1. 初始化项目

用户说"新建小说"/"写小说"时：

1. 询问：标题、类型、核心设定、主要人物（至少主角+一个关键角色）
2. 运行：`python3 <skill_dir>/scripts/init_project.py <项目目录> --title <标题> --genre <类型>`
3. 将用户提供的世界观和人物写入 `world.json` 和 `characters.json`
4. **引导用户定大纲**：先列出 5-10 章的粗略大纲，写入 `outlines/outline.md`
5. 大纲确认后再开始写第一章

### 2. 写章节（核心流程）

#### 2a. 加载上下文（必须，不可跳过）

读取以下文件（不存在则跳过）：
- `characters.json` — 人物性格、关系、战斗风格
- `world.json` — 世界观、力量体系、势力、规则
- `style_constraints.json` — 避雷词库（**必读，写入时严格禁止使用其中的词**）
- `outlines/outline.md` — 大纲，确认本章要写什么
- `outlines/summary_log.md` — 前文摘要，保持连贯
- 最近 1-2 章正文（`chapters/` 目录最新的 .md 文件）

#### 2b. 确定场景类型

根据大纲和内容自动判断，或由用户指定：
- `combat` — 打斗/战斗
- `emotion` — 情感/互动
- `suspense` — 悬疑/推理
- `general` — 通用叙事

#### 2c. 字数控制

- 默认每章 **2500 字**（中文字符）
- 用户可自定义："写 3000 字" / "写短一点"
- 写完后用 `wc -m` 检查字数，不足则补写，超出则告知

#### 2d. 分段写作（P1 中断机制）

不要一口气写完全章。按以下节奏：

1. **先写开头 500 字**，然后暂停，问用户："开头方向对吗？继续？"
2. 用户确认后，写到 **1500 字**，再暂停，问："节奏和文风 ok 吗？"
3. 用户确认后，**写完全章**

如果用户说"直接写完"或"不用停"，则跳过中断，一口气写完。

#### 2e. 续写模式

当用户说"继续写"或"写下一章"时：

1. 读取上一章最后 500 字
2. 读取 `summary_log.md` 获取剧情位置
3. 读取 `outline.md` 确认下一章内容
4. **无缝衔接**：不要重复上一章的结尾，直接从新的场景或时间点切入

#### 2f. 生成正文

**写作铁律（不可违反）：**
1. `style_constraints.json` 中的避雷词，一个都不能出现
2. 遵守下方的场景文风规则
3. 保持人物性格一致（写对话前查 characters.json）
4. 保持剧情连贯（参考 summary_log.md）
5. 开头从动作或对话切入，不要从大段环境描写开始
6. 章节结尾必须留悬念或情绪余韵

#### 2g. 自进化反思（写完后自动执行）

**写完每章后，必须执行以下步骤：**

1. 运行自检脚本：
   ```bash
   python3 <skill_dir>/scripts/self_check.py <项目目录>/chapters/<最新章节>.md <项目目录>/style_constraints.json
   ```
2. 脚本会自动扫描避雷词和AI味表达，输出报告
3. 如有新发现的避雷词，自动追加到 `style_constraints.json`
4. 将本章摘要（2-3 句话）追加到 `outlines/summary_log.md`
5. 将自检结果告知用户："本章避雷词检查通过/发现 X 个问题，已修正"

### 3. 人物管理

- **添加**：更新 `characters.json`
- **查询**：读取并展示
- **修改**：直接编辑
- **一致性检查**：写新章前，检查人物是否 OOC

### 4. 世界观管理

- **添加**：更新 `world.json`
- **查询**：读取并展示
- **一致性检查**：写新章前，检查是否与已有设定冲突

## 场景文风规则（内联）

### ⚔️ 打斗 (combat)
- **短句为王**：单句不超 15 字，制造紧迫感
- **动词密度 +30%**：砍冗余形容词
- **感官轰炸**：听觉（兵刃交接）、触觉（劲风）、视觉（剑光血雾）
- **节奏**：三短一长，制造呼吸感
- **禁止**："他感到一阵剧痛"→直接写身体反应；"战斗异常激烈"→写具体交锋细节

### ❤️ 情感 (emotion)
- **微表情**：指尖绞衣角、睫毛轻颤、耳尖泛红
- **环境烘托**：雨声、烛火、暮色替人物说话
- **节奏放慢**：拉长句子，增加心理独白
- **留白**：不说破，让读者自己感受
- **禁止**："他很伤心"→写身体反应和环境；"气氛很尴尬"→写沉默和各自动作

### 🔍 悬疑 (suspense)
- **视角限制**：只写角色能看到的
- **留白暗示**：不说破危险，用细节暗示
- **心理压迫**：让读者跟着角色紧张
- **禁止**："他感到毛骨悚然"→写具体生理反应；"真相是..."→通过线索让读者推理

### 📖 通用 (general)
- **简洁**：3 字能说清的不用 10 字
- **节奏感**：长短句交替
- **画面感**：具体动作代替抽象描述
- **信息密度**：每段有推进，拒绝水字数
- **对话**：要有信息量、潜台词、冲突或推动

## AI 味避雷手册（核心）

### 高频避雷词（在 style_constraints.json 基础上额外检查）
"不仅"、"总之"、"仿佛"、"显然"、"毫无疑问"、"事实上"、"与此同时"、"换言之"、"可以说"、"某种程度上"、"值得一提"、"不言而喻"、"众所周知"

### AI 常见病
1. **解释癖**：❌ "他这样做是因为童年经历..." → ✅ 直接写经历
2. **情绪标签化**：❌ "她感到悲伤" → ✅ "她别过脸去。窗外的雨大了。"
3. **过度修饰**：❌ "他用深邃锐利的眼眸凝视远方被夕阳染红的天际" → ✅ "他看着夕阳。"
4. **万能比喻**：❌ "仿佛xxx一般"过度使用 → ✅ 用新颖比喻或不比喻
5. **无效对话**：❌ "你怎么了""我没事" → ✅ 对话要有信息量
6. **叙述代替描写**：❌ "他们打了激烈一仗" → ✅ 写具体交锋

### 自查关键词
写完后搜索正文：`不仅`、`总之`、`仿佛`、`感到`、`非常`、`突然`、`一阵`

## 关键原则

1. **当"代笔作家"，不当"AI写手"** — 写出来要像人写的
2. **上下文是命脉** — 不读前文不写新章
3. **避雷词零容忍** — style_constraints.json 里的词，一个不出现
4. **进化是核心** — 每次写完自检，把教训记下来
5. **人物一致性** — 写对话前查人物性格
6. **大纲驱动** — 不要写到哪算哪，按大纲推进
